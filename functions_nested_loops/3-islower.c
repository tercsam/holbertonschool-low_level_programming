#include <stdio.h>

/**
 * _islower - Entry point
 *
 * @c - Checked character
 *
 * Return: 0 (Success)
 */

int _islower(int c)

{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
		else if
		{
			return (0);
		}

}
