#include <stdio.h>

/**
 * _islower - Entry point
 * @c: The character to be checked.
 *
 * Return: 0 (Success)
 */

int _islower(int c)

{
	if (c >= 'a' && c <= 'z')
	return (1);

		else

			return (0);


}
